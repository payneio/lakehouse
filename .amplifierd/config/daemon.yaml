# amplifierd daemon configuration
# This configures the daemon transport layer only
# For amplifier-core configuration, see amplifier-core docs

# Server settings
host: "127.0.0.1"
port: 8420
log_level: "info"
workers: 1

data_path: "/data"

force_collection_refresh_on_start: False
auto_profile_build_on_start: True
force_profile_rebuild_on_start: False
