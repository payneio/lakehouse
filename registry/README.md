# Amplifier Component Registry

This registry serves two purposes, first, to have a set of minimum default collection components that make the Amplifier app functional. Second, to provide copies of external collection components (namely profiles) that do not follow the Profile Schema version 2 that is required by amplifierd.

## External sources

- git+https://github.com/microsoft/amplifier-collection-recipes@main
