# Production build: the frontend is served as a static site on its own domain
# (lakehouse.civil.payne.io) and calls the API on a separate domain. Vite bakes
# this in at build time (`pnpm run build`), overriding .env / .env.local.
VITE_API_URL=https://lakehouse-api.civil.payne.io
